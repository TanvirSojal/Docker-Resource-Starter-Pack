version: '3.9'

services:
  minio:
    image: minio/minio:latest
    restart: unless-stopped
    ports:
      - '9000:9000'
      - '9001:9001'
    env_file:
      - .env
    volumes:
      - ./data:/data
    command: server /data --console-address ":9001"
